{
  "buildCommand": "sh -c 'if [ -n \"$DATABASE_URL\" ]; then npx prisma migrate deploy --schema prisma/schema.pg.prisma; else echo \"Skipping prisma migrate deploy (no DATABASE_URL at build time)\"; fi && npx prisma generate --schema prisma/schema.pg.prisma && next build'"
}